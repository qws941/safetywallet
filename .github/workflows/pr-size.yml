# PR Size Labeler
# SSoT: qws941-lab/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _pr-size.yml

name: PR Size

on:
  pull_request_target:
    types: [opened, synchronize]

permissions:
  pull-requests: write

jobs:
  pr-size:
    uses: qws941-lab/.github/.github/workflows/_pr-size.yml@master
