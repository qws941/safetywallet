# PR Labeler
# SSoT: qws941-lab/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _labeler.yml

name: PR Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  labeler:
    uses: qws941-lab/.github/.github/workflows/_labeler.yml@master
